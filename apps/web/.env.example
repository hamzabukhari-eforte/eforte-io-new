# Web-only env (ports live in the repo root `.env`)

# Keep URL ports in sync with root WEB_PORT / API_PORT / STRAPI_PORT
NEXT_PUBLIC_SITE_URL=http://localhost:6200
NEXT_PUBLIC_API_URL=http://localhost:6100
NEXT_PUBLIC_STRAPI_URL=http://localhost:6400
